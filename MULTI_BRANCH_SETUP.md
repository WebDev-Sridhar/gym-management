Explicitly Phase E (out of scope v1, in the plan)

RLS-level branch isolation (currently RLS is gym-only; branch filtering is app-layer)
branch_manager / receptionist roles
Cross-branch member transfers (no UI; needs manual SQL today)
Public gym website "Locations" section per branch
Branch-specific subdomain/custom domain